# Test dependencies for the trading framework

# Core testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Async testing (for future real-time tests)
pytest-asyncio>=0.21.0

# Test reporting
pytest-html>=3.2.0

# Code coverage
coverage>=7.2.0

# Data manipulation (already in main requirements, but listed for completeness)
pandas>=2.0.0
numpy>=1.24.0
